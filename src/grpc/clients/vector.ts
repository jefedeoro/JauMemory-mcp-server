import { AuthManager } from "../../auth/AuthManager.js";

export class VectorServiceClient {
  constructor(_address: string, _authManager: AuthManager, _useTls: boolean = true) {
    // Stub implementation
  }
}
